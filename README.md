# Inverse Entropic Optimal Transport Solves Semi-supervised Learning via Data Likelihood Maximization

The main code is located in the `.ipynb` files within the `notebooks` directory. All other code serves as supporting functionality for running these notebooks.

## Legacy branch (`legacy/ebieot-nn`)

This branch preserves the pre-Hydra **EgEOT / energy-based IOT** layout (`configs/`, `src/models/`, flat notebooks). Use the **`main`** branch for the current Hydra layout (`conf/`, `src/ebieot/`). If something regresses on `main`, check out `legacy/ebieot-nn` and see [LEGACY.md](LEGACY.md) for which notebook or script to run.

Preservation tags: `legacy/ebm` (pre-docs EBM tip), `legacy/ebieot-nn` (documented tip).
