# Standalone Langevin overrides (e.g. ``ebieot.model.num_iterations=50``)
thresh: null
step_size: 0.05
noise: 0.05
num_iterations: 100
decay: 1.0
grad_proj_type: none
norm_thresh: 1.0
value_thresh: 0.01
projection:
  min: 0.0
  max: 1.0
  is_projected: false
