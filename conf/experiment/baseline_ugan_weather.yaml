# @package _global_
defaults:
  - /baseline/train@train: ugan
  - /baseline/model@model: ugan
  - /dataset: weather/baseline
  - _self_

method: ugan
task_name: train_baseline

train:
  steps_to: 10000
  batch_size: 128
