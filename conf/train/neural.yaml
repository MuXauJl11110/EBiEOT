defaults:
  - optimizer/paired@optimizer.paired: neural
  - optimizer/unpaired@optimizer.unpaired: neural
  - _self_

name: Neural-Based_Swiss_Roll
seed: 42
paired_batch_size: 1024
unpaired_batch_size: 128
gradient_max_norm: .inf
steps_from: 0
steps_to: 1000
ema_update: false
log_every: 50
plot_every: 10
